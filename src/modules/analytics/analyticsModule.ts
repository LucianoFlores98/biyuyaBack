import { DependencyManager } from "../../dependencyManager";

export const AnalyticsModuleInitializer = (dependencyManager: DependencyManager) => {
  // Analytics no tiene repositorios propios
  // Solo consume servicios de otros módulos
};
